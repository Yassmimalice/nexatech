/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'randomuser.me',
      'avatars.githubusercontent.com'
    ],
  },
}

module.exports = nextConfig

